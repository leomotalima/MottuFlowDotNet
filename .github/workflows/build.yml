name: Build and Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '7.0.x'

      - name: Restore dependencies
        run: dotnet restore MottuFlowDotNet/MottuFlowDotNet

      - name: Build
        run: dotnet build MottuFlowDotNet/MottuFlowDotNet --no-restore

      - name: Test
        run: dotnet test MottuFlowDotNet/MottuFlowDotNet --no-build --verbosity normal
